
<h3>FIRST STEP OF CHECKOUT: collect email and address </h3>



<% if !is_logged_in %>
Returning customer?   <a href="">Sign in </a>for faster checkout.
<% end %>

this used to use @order but now it uses the current_order helper
<%= form_for current_order, :url => url_for(path: update_address_path, method: :post)   do |f| %>





    <h2> Shipping Address </h2>
<%= f.fields_for :address,  @address do | addr | %>
    <%= addr.label :recipient_name %>
    <%= addr.text_field :recipient_name %><br>
    
    <%= addr.label :street %>
    <%= addr.text_field :street %><br>
 
    <%= addr.label :city %>
    <%= addr.text_field :city %><br>
 
    <%= addr.label :state %>
    <%= addr.text_field :state %><br>

    <%= addr.label :country %>
    <%= addr.text_field :country %><br>

    <%= addr.label :zipcode %>
    <%= addr.text_field :zipcode %><br>
 

<% end %>
<%= f.submit "Continue to Shipping" %>

<% end %>